Azorult:
  description: runs Azorult parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule: # Must not have empty or nonexistent fields
      - ./yara_rules/azorult.yara
  parser: 
      - MWCP:
         - Azorult
BitPaymer:
  description: runs BitPaymer parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/bitpaymer.yara
  parser: 
      - MWCP:
         - BitPaymer
ChChes:
  description: runs ChChes parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/chches.yara
  parser: 
      - MWCP:
         - ChChes
DoppelPaymer:
  description: runs DoppelPaymer parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/doppelpaymer.yara
  parser: 
      - MWCP:
         - DoppelPaymer

Emotet:
  description: yara rule that runs emotet parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/emotet.yara
    tag:
      - ./tag_rules/emotet.rule
  parser: 
      - MWCP:  
        - Emotet

Enfal:
  description: yara rule that runs enfal parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/enfal.yara
  parser: 
      - MWCP:
         - Enfal
EvilGrab:
  description: runs EvilGrab parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/evilgrab.yara
  parser: 
      - MWCP:
         - EvilGrab
HttpBrowser:
  description: runs HttpBrowser parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/httpbrowser.yara
  parser: 
      - MWCP:
         - HttpBrowser
IcedID:
  description: runs IcedID parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/icedid.yara
  parser: 
      - MWCP:
         - IcedID
RCSession:
  description: runs RCSession parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/rcsession.yara
  parser: 
      - MWCP:
         - RCSession
RedLeaf:
  description: runs RedLeaf parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/redleaf.yara
  parser: 
      - MWCP:
         - RedLeaf
Redsip:
  description: runs Redsip parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/redsip.yara
  parser: 
      - MWCP:
         - Redsip
Retefe:
  description: runs Retefe parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/retefe.yara
  parser: 
      - MWCP:
         - Retefe
SmokeLoader:
  description: runs SmokeLoader parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule:
      - ./yara_rules/smokeloader.yara
  parser: 
      - MWCP:
         - SmokeLoader
QakBot:
  description: runs QakBot parser when hit
  classification: 'TLP:W'
  selector:
    yara_rule: # Must not have empty or nonexistent fields
      - ./yara_rules/qakbot.yara
  parser:
      - MWCP:
         - QakBot